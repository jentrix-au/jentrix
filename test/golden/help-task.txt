Usage: jentrix task [options] [command]

archive, create, find-similar, get, list, move, … (11 commands)

Options:
  -h, --help               display help for command

Commands:
  archive [options]        Archive a task (soft delete — it disappears from the board but is
                           recoverable).
  create [options]         Create a new task in a column.
  find-similar [options]   Find tasks SEMANTICALLY similar to an existing task (pass taskId) or to
                           free te…
  get [options]            Fetch full details of a task including description, labels, assignees,
                           subtasks…
  list [options]           List tasks in a board or column with board/column names, total count, and
                           pagin…
  move [options]           Move a task to a different column (= change status on the kanban board)
                           and/or …
  search [options]         Search tasks workspace-wide across all boards: exact task key (for
                           example STK-…
  set-assignees [options]  Set the full list of assignees on a task.
  set-labels [options]     Set the full list of labels on a task.
  update [options]         Update fields on an existing task.
  update-review [options]  Create or update review request metadata.
  help [command]           display help for command
