{
  "name": "acme-jentrix-claude",
  "version": "0.1.0",
  "private": true,
  "description": "Reference Claude Code extension for Jentrix (examples/acme-claude). The peerDependencies range is the CLI range this plugin was written against; `pnpm validate:examples` checks it.",
  "license": "MIT",
  "peerDependencies": {
    "@jentrix/cli": ">=0.7.0 <1.0.0"
  }
}
